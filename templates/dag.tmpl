{% for import in imports %}
{{import}}
{% endfor %}

default_args = {
    'owner': 'airflow',
}

with DAG(
    dag_id="{{dag_id}}",
    start_date=datetime(2021, 1, 1),
    schedule="@daily",
) as dag:

# DAG Tasks

{% for task in tasks %}
    {{task}}
{% endfor %}

# Task Dependencies

{% for dependency in dependencies %}
    {{dependency}}
{% endfor %}